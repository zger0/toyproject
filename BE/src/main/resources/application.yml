spring:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
#        show_sql: true
  logging.level:
    org.hibernate.SQL: debug
#    org.hibernate.jdbc.bind : trace

  security:
    user:
      name: user
      password: password